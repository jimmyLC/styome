<h1>show girl</h1>
<% @girls.each do |g| %>
  <%= link_to g.name, girl_path(g) %>
<% end %>

<h1>show product</h1>
<% @products.each do |p| %>
  <div>
    <%= render_product_photo(p.main_photo) %>
    <%= link_to p.title, product_path(p) %>
<!--     <%= p.description %>
    <%= p.price %>
    <%= p.quantity %> -->
    <%= link_to "edit", edit_product_path(p) %>
    <%= link_to "destroy", product_path(p), method: :delete %>
    <%= button_to "加入購物車", cart_path(product_id: p.id, quantity: 1) %>
  </div>
<% end %>

<h1>create product</h1>
<%= render "form" %>
<%= render "/common/cart" %>