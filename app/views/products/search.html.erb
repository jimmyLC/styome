<h1>Product Search</h1>

<div class="pin">
<ul>
  <% @products.each do |product| %>
  <li>
    <%= link_to (image_tag product.avatar.url), product_path(product) %>
  </li>
  <li>
    <%= link_to product.title, product_path(product.id) %>
  </li>
  <% end %>
</ul></div>